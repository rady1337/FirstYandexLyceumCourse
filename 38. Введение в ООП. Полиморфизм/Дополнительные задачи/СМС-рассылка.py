class Person:    def __init__(self, name, sec_name, surname, phones):        self.name = name        self.sec_name = sec_name        self.surname = surname        self.phones = phones    def get_phone(self):        return self.phones.get('private')    def get_work_phone(self):        return self.phones.get('work')        def get_name(self):        name = self.surname + ' ' + self.name + ' ' + self.sec_name        return name    def get_sms_text(self):        text = f'Уважаемый {self.name} {self.sec_name}! '        text += f'Примите участие в нашем беспроигрышном конкурсе для физических лиц'        return textclass Company:    def __init__(self, company_name, company_type, company_phones, *names):        self.company_name = company_name        self.company_type = company_type        self.company_phones = company_phones        self.names = names    def get_phone(self, key='contact'):        if key in self.company_phones:            return self.company_phones.get(key)        for person in self.names:            number = person.get_work_phone()            if number:                return number    def get_name(self):        return self.company_name    def get_sms_text(self):        text = f'Для компании {self.company_name} есть супер предложение! '        text += f'Примите участие в нашем беспроигрышном конкурсе для {self.company_type}'        return textdef send_sms(*obj):    for ob in obj:        number = ob.get_phone()        if number:            print(f'Отправлено СМС на номер {number} с текстом: {ob.get_sms_text()}')        else:            print(f'Не удалось отправить сообщение абоненту: {ob.get_name()}')                    