class Table:    def __init__(self, height, width):        self.rows = height        self.cols = width        self.table = []        for i in range(self.rows):            vlozh = []            for j in range(self.cols):                vlozh.append(0)            self.table.append(vlozh)    def n_rows(self):        return self.rows    def n_cols(self):        return self.cols    def set_value(self, row, col, value):        self.table[row][col] = value    def get_value(self, row, col):        if row in range(self.rows) and col in range(self.cols):            return self.table[row][col]        return    def day(self):        print(self.table)