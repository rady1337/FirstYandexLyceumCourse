class RightParagraph:    def __init__(self, weight):        self.weight = weight        self.right = []    def add_word(self, word):        self.right.append(word)    def end(self):        line = ''        for i in self.right:            if not line:                line += i            elif len(line) + len(i) + 1 <= self.weight:                line += ' ' + i            else:                print(line.rjust(self.weight, ' '))                line = i        print(line.rjust(self.weight, ' '))        self.right.clear()class LeftParagraph:    def __init__(self, weight):        self.weight = weight        self.left = []    def add_word(self, word):        self.left.append(word)    def end(self):        line = ''        for i in self.left:            if not line:                line += i            elif len(line) + len(i) + 1 <= self.weight:                line += ' ' + i            else:                print(line)                line = i        print(line)        self.left.clear()