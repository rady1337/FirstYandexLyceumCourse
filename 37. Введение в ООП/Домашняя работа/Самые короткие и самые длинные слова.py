class MinMaxWordFinder:    def __init__(self):        self.words = []    def add_sentence(self, word):        word = word.split(' ')        for i in word:            if i.isspace() or i == '':                pass            else:                i = i.strip()                self.words.append(i)    def shortest_words(self):        self.short = []        min_len = 100        for i in self.words:            if len(i) < min_len:                min_len = len(i)        for i in self.words:            if len(i) == min_len:                self.short.append(i)        self.short.sort()        return self.short    def longest_words(self):        self.long = []        max_len = 0        for i in self.words:            if len(i) > max_len:                max_len = len(i)        for i in self.words:            if len(i) == max_len:                self.long.append(i)        self.long = set(self.long)        self.long = list(self.long)        self.long.sort()        return self.long