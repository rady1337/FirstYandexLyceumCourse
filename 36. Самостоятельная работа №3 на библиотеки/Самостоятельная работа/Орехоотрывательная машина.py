from PIL import Imagedef grab_a_nut(image, color):    if color == (255, 0, 0):        return -500    else:        pixels = image.load()        w, h = image.size        sum = 0        cnt = 0        count = []        for i in range(w):            stroka_count = 0            for j in range(h):                r, g, b = pixels[i, j]                if r == int(color[0]) and g == int(color[1]) and b == int(color[2]):                    stroka_count += 1            count.append(stroka_count)        index = count.index(max(count))        # print(index)        formula = index * 1001 // w - 500        return int(formula)# image = Image.open(input())# color = tuple(map(int, input().split()))# print(grab_a_nut(image, color))