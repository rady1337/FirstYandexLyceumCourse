import datetimedef observation_day(*dates):    datess = []    for i in dates:        i = i.split('-')        dateee = datetime.date(int(i[0]), int(i[1]), int(i[2]))        datess.append(dateee)    date = []    for i in datess:        if i.day % 3 != 0:            date.append(i)    now = datetime.date.today()    min = 1000    den = 0    for i in date:        delta = (now - i).days        if int(delta) < min and int(delta) >= 0:            min = delta            den = i    nedeli = i.weekday()    return (min, nedeli + 1)