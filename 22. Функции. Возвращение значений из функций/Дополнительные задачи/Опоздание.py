def to_seconds(val):    if ':' not in str(val):        return int(val) * 60    val = val.split(':')    return int(val[0]) * 3600 + int(val[1]) * 60def constuctor(seconds):    return f"{seconds // 3600}:{seconds % 3600 // 60 if seconds % 3600 // 60 != 0  else '00'}"def sum_time(first, second):    return constuctor(to_seconds(first) + to_seconds(second))def late(now, classes, bus):    if to_seconds(max(bus)) + to_seconds(now) + 300 < to_seconds(classes):        return f"Выйти через {max(bus) - 5} минут"    for i in reversed(bus):        if to_seconds(i) - 300 + to_seconds(now) + to_seconds(20) <= to_seconds(classes):            return f"Выйти через {i - 5} минут" if i - 5 >= 0 else "Опоздание"    return "Опоздание"