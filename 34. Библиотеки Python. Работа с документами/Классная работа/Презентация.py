from pptx import Presentationfrom pptx.util import Inches, Ptfrom pptx.enum.text import PP_ALIGNfrom pptx.dml.color import RGBColorimport randomprs = Presentation()slide = prs.slides.add_slide(prs.slide_layouts[0])title = slide.shapes.titletitle.text = 'модуль рандом'methods = filter(lambda x: not x.startswith('_') and x.islower(), dir(random))for m in methods:    slide = prs.slides.add_slide(prs.slide_layouts[6])    left = top = width = height = Inches(1)    title = slide.shapes.add_textbox(left, top // 4, width * 8, height)    tf = title.text_frame    p = tf.add_paragraph()    p.text = m    p.font.size = Pt(24)    p.font.name = 'Courier New'    p.alignment = PP_ALIGN.CENTER    text = slide.shapes.add_textbox(left // 2, top, width * 9, height * 6)    tf = text.text_frame    p = tf.add_paragraph()    p.text = str(eval('random.' + m + '.__doc__'))    p.font.name = 'Courier New'    p.font.size = Pt(14)    p.alignment = PP_ALIGN.JUSTIFY    p.font.color.rgb = RGBColor(122, 122, 122)prs.save('res.pptx')