import pymorphy2import sysdef replace_all(value, key='!@#"$%^&*?;:.,+-=/[]|\><»«\t\n'):    return ''.join(list(map(lambda x: ' ' if x in key else x, value)))morph = pymorphy2.MorphAnalyzer()text = replace_all(''.join(sys.stdin.readlines()).lower())frequency = {}for i in map(lambda x: morph.parse(x)[0].normal_form,             filter(lambda t: 'NOUN' in morph.parse(t)[0][1] and morph.parse(t)[0][3] > 0.5, text.split())):    if i in frequency:        frequency[i] += 1    else:        frequency[i] = 1frequency = dict(sorted(frequency.items(), key=lambda x: (x, frequency[x[0]])[0], reverse=True))frequency = dict(sorted(frequency.items(), key=lambda x: (x, frequency[x[0]])[1], reverse=True))print(' '.join(list(frequency)[:10]))