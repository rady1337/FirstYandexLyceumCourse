import sysfrom random import choice as chuse = []al = []friends = []for lin in sys.stdin:    al.append(lin.strip())for j in al:    para = [j, '-']    while True:        chel = ch(al)        if chel != j and chel not in use:            para.append(chel)            use.append(chel)            break    friends.append(para)for i in friends:    for j in i:        print(j, end=' ')    print()