user_dict = {}class MailClient:    def __init__(self, server, user):        self.server = server        self.user = user        if server in user_dict:            d = user_dict[server]            if user not in d:                d[user] = []                user_dict[server] = d        else:            user_dict[server] = {user: []}    def receive_mail(self):        if user_dict[self.server][self.user] != []:            mess = user_dict[self.server][self.user]            if len(mess) == 1:                text = 'У вас 1 новое письмо:\n' + mess[0]            else:                text = 'У вас есть новые письма:\n' + '\n'.join(mess)            user_dict[self.server][self.user] = []        else:            text = 'У вас нет новых писем.'        return text        def send_mail(self, server1, user1, message):        if server1 in user_dict:            d = user_dict[server1]            if user1 in d:                list_mes = user_dict[server1][user1]                list_mes.append(message)                user_dict[server1][user1] = list_mes                text = f'Сообщение - "{message}". Пользователь - {user1}. Сервер - {server1}'            else:                text = 'ОШИБКА! Такого пользователя не существует!'        else:            text = 'ОШИБКА! Такого сервера не существует!'        return text    def server_list(self):        return 'Список доступных серверов: ' + ', '.join(user_dict.keys())        while True:    print('''    |----------------------|    |   Почтовый  Клиент   |    |           От         |    |          Ильи        |    |        Федорова      |    |----------------------|    ''')    serv = input('Введите сервер: ')    name = input('Введите имя: ')    client = MailClient(serv, name)    choose = '''\tМеню:    |--------------------------|    |   1) Просмотреть почту   |    |--------------------------|    |   2) Отправить  письмо   |    |--------------------------|    |   3) Список серверов     |    |--------------------------|    '''    while True:        print(choose)        choose_inp = input('Выберите действие (1 / 2 / 3): ')        if choose_inp == '1' or choose_inp == '2' or choose_inp == '3':            break        else:            print('ОШИБКА! Введенных действий нет!')    if choose_inp == '1':        print(client.receive_mail())    elif choose_inp == '2':        print('Список доступных серверов: ' + ', '.join(user_dict.keys()))        server_send = input('Введите сервер для отправки: ')        user_send = input('Введите имя пользователя, которому отправляете: ')        mes_send = input('Введите сообщение для отправки: ')        print(client.send_mail(server_send, user_send, mes_send))    elif choose_inp == '3':        print(client.server_list())    choose_ext = '''    |--------------------------|    |   1) Остатья в клиенте   |    |--------------------------|    |   2) Выйти из клиента    |    |--------------------------|    '''    while True:        print(choose_ext)        choose_inp = input('Выберите действие (1 / 2): ')        if choose_inp == '1' or choose_inp == '2':            break        else:            print('ОШИБКА! Введенных действий нет!')    if choose_inp == '2':        breakprint('Выход из почтового клиента...')